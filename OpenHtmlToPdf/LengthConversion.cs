namespace OpenHtmlToPdf
{
    public static class LengthConversion
    {
        public static Length Millimeters(this double length)
        {
            return Length.Millimeters(length);
        }

        public static Length Centimeters(this double length)
        {
            return Length.Centimeters(length);
        }

        public static Length Inches(this double length)
        {
            return Length.Inches(length);
        }

        public static Length Millimeters(this float length)
        {
            return Length.Millimeters(length);
        }

        public static Length Centimeters(this float length)
        {
            return Length.Centimeters(length);
        }

        public static Length Inches(this float length)
        {
            return Length.Inches(length);
        }

        public static Length Millimeters(this int length)
        {
            return Length.Millimeters(length);
        }

        public static Length Centimeters(this int length)
        {
            return Length.Centimeters(length);
        }

        public static Length Inches(this int length)
        {
            return Length.Inches(length);
        }
    }
}
